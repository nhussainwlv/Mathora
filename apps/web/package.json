{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "description": "Mathora static HTML/CSS/JS PWA",
  "scripts": {
    "generate:curriculum": "tsx scripts/generate-curriculum.ts",
    "dev": "npx serve public -l 3000",
    "build": "node -e \"console.log('Static site ready in public/')\"",
    "start": "npx serve public -l 3000"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5"
  }
}
